Solve the locally stored Robots Evolved **intermediate Stacking** lesson. Robots slide until stopped by walls or robots. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot can stop on the wildcard goal.

Use `robot-game show` and `robot-game test BN,RW,...` offline. Write at most 20 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: repeatedly reposition stopping robots to build a route toward the middle.