Solve the locally stored Robots Evolved **Stacking 2** lesson. Robots slide until a wall or another robot stops them. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot may reach the wildcard goal.

Use `robot-game show` and `robot-game test GE,BE,...` offline. Write at most 20 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: position one robot as a stopping wall, then stack another robot against it at the goal.