Solve the locally stored Robots Evolved **beginner Stacking** lesson. Robots slide in cardinal directions until stopped by a wall or another robot. Moves combine `B/G/R/Y` with `N/E/S/W`; any robot may stop on the wildcard goal.

Use `robot-game show` and `robot-game test BS,BE,...` offline. Write at most 20 moves as a JSON array to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: robots can serve as movable walls that stop another robot beside the goal.