Solve the locally stored Robots Evolved **Shifting Bots** lesson. Robots slide until stopped by walls or robots. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot may reach the wildcard goal.

Use `robot-game show` and `robot-game test RS,YW,...` offline. Write at most 20 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: shift one bot past another, move the pivot, and repeat until aligned with the goal.