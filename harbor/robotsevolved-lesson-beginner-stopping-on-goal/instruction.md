Solve the locally stored Robots Evolved **Stopping on Goal** lesson. A robot passes over the goal unless something stops it there. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot can solve the wildcard goal.

Use `robot-game show` and `robot-game test GS,RS,...` offline. Write at most 20 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: arrange a robot or wall immediately beyond the goal before sending the solving robot toward it.