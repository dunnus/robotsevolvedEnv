Solve the locally stored Robots Evolved **Group Movement** lesson. Robots slide until stopped by walls or robots. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot may reach the wildcard goal.

Use `robot-game show` and `robot-game test YN,BE,...` offline. Write at most 70 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: combine stacking, shifting, and rotating pivots to move the four-robot group across the open board.