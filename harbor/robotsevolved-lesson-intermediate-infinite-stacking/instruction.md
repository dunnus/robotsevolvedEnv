Solve the locally stored Robots Evolved **Infinite Stacking** lesson. Robots slide until stopped by a wall or robot. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot may reach the wildcard goal.

Use `robot-game show` and `robot-game test GS,GW,...` offline. Write at most 46 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: use one robot as a pivot and cycle the other robots around it to shift the group toward the goal.