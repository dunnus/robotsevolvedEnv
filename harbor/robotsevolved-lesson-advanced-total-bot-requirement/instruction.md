Solve the locally stored Robots Evolved **Total Bot Requirement** lesson. Robots slide until stopped by walls or robots. Moves combine `B/G/R/Y` and `N/E/S/W`; any robot may reach the wildcard goal.

Use `robot-game show` and `robot-game test BN,YW,...` offline. Write at most 43 moves as JSON to `/workspace/solution.json`. Do not modify the puzzle or simulator.

Lesson hint: route the group through the narrow region so robots can continue acting as pivots and stopping walls.